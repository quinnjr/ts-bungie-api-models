/**
 * A hint for how the presentation node should be displayed when shown in a 
 * list. How you use this is your UI is up to you.
 */
export enum DisplayStyle {
  Category,
  Badge,
  Medals,
  Collectible,
  Record
}
